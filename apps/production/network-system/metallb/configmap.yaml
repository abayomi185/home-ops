apiVersion: v1
kind: ConfigMap
metadata:
  name: metallb-helm-chart-value-overrides
data:
  values.yaml: |-
    # L2 mode only — no BGP, no FRR.
    speaker:
      frr:
        enabled: false
    crds:
      enabled: true
      validationFailurePolicy: Fail
