apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: intel-device-plugin-gpu
  namespace: kube-system
spec:
  values:
    tolerations:
      - key: dedicated
        operator: Equal
        value: gpu
        effect: NoSchedule
