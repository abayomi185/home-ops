apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: node-feature-discovery
  namespace: kube-system
spec:
  values:
    worker:
      tolerations:
        - key: dedicated
          operator: Equal
          value: gpu
          effect: NoSchedule
