apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: prowlarr
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`prowlarr.internal.${DOMAIN}`)
      kind: Rule
      services:
        - name: prowlarr-service
          port: 9696
  tls:
    certResolver: letsencrypt
