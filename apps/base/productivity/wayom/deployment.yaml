apiVersion: apps/v1
kind: Deployment
metadata:
  name: &deploymentName wayom-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: *deploymentName
  template:
    metadata:
      labels:
        app: *deploymentName
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: wayom
          image: ghcr.io/abayomi185/wayom:latest@sha256:9bb5e5cb260713f52b8a0d0884e3b6ee493fa75a08771dadaefd5a294cddaa6a # renovate: datasource=docker depName=ghcr.io/abayomi185/wayom
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3080
              name: http
              protocol: TCP
          env:
            - name: HOST
              value: "0.0.0.0"
            - name: PORT
              value: "3080"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: wayom-secrets
                  key: database_url
            - name: WAYOM_CONFIG_PATH
              value: /config/wayom.toml
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: wayom-secrets
                  key: jwt_secret
            - name: GITHUB_OAUTH_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: wayom-secrets
                  key: github_oauth_client_id
            - name: GITHUB_OAUTH_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: wayom-secrets
                  key: github_oauth_client_secret
            - name: GITHUB_OAUTH_REDIRECT_URL
              valueFrom:
                secretKeyRef:
                  name: wayom-secrets
                  key: github_oauth_redirect_url
            - name: LUNCH_FLOW_API_KEY
              valueFrom:
                secretKeyRef:
                  name: wayom-secrets
                  key: lunch_flow_api_key
          volumeMounts:
            - mountPath: /config
              name: config
              readOnly: true
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
      volumes:
        - name: config
          configMap:
            name: wayom-config
